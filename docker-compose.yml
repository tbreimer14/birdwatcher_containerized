services:
  birdwatcher:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - birdwatcher-data:/app/data
    environment:
      HF_HOME: /app/data
    restart: unless-stopped

volumes:
  birdwatcher-data:
